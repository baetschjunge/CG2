/*
 * JavaScript / Canvas teaching framwork
 * 
 */

/* requireJS module definition */
define(["three"],
    (function(THREE) {

        "use strict";

        /**
         *
         * @param scene  - reference to the scene
         * @constructor
         */
        var Ellipsoid = function (config) {

            config = config || {};
            var uMin;
            var uMax;
            var vMin;
            var vMax;
            var uSegments;
            var vSegments;
            this.drawStyle;


/*magic of ellipsoid
*/
        };

/*
            this.getPositions = function() {
                return this.positions;
            };

            this.getColors = function() {
                return this.colors;
            };
*/

        return Ellipsoid;
    }));
    
